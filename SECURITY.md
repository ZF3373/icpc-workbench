# 安全策略（Security Policy）

## 支持的版本

| 版本 | 支持情况 |
| --- | --- |
| v0.4.x | ✅ 接收安全报告 |
| < v0.4.0 | ❌ 请先升级 |

## 如何报告漏洞

**请勿在公开 Issue 中披露安全漏洞。**

请使用 GitHub 的私密漏洞报告入口：
仓库页面 → **Security** 标签 → **Report a vulnerability**（私密安全建议），
我们会尽快回复并跟进修复。

报告时请尽量包含：

- 受影响的版本（软件「设置 → 软件更新」处可见）
- 复现步骤或概念验证
- 影响评估（数据泄露 / 提权 / 拒绝服务等）

## 安全设计边界

了解以下边界有助于合理评估问题：

- 本软件为**本地单机应用**：服务仅监听 `127.0.0.1`，不上传任何数据到云端
- 平台同步只读取各 OJ 的**公开数据**；洛谷 Cookie 仅保存在本机数据库，不参与任何传输
- 桌面版（v0.4.0+）采用 Tauri 壳 + 本地核心双进程，核心仅接受本机回环连接

## 关于代码签名

本软件未购买商业代码签名证书，首次运行 Windows SmartScreen 可能提示
"已保护你的电脑"（「更多信息」→「仍要运行」）。发布附件附带
`checksums.sha256`，安装/运行前可自行校验文件完整性：

```powershell
Get-FileHash -Algorithm SHA256 .\icpc-workbench.exe
```

## 已知缓解措施

- 同步与赛事拉取均使用平台公开 API，不存储任何账号密码
- AI 功能的 API Key 仅保存在本机数据库（用户自行填写），默认关闭
- 所有外部请求均设置超时；对反爬平台（洛谷）使用独立的挑战处理，失败即降级
